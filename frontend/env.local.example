# Frontend Environment Example
# Copy to .env.local in the frontend folder

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
# Optional: For client-side features that call NASA directly
NEXT_PUBLIC_NASA_API_KEY=
# Required: Mapbox token for location picker and meme generator
NEXT_PUBLIC_MAPBOX_TOKEN=
